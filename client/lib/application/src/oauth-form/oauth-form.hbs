<h2>OAuth config for {{application.name}}</h2>
<div class="btn-group">
    <a href="/application/detail/{{application.id}}" class="btn btn-default">
        <i class="fa fa-chevron-left"></i> Back to {{application.name}}
    </a>
</div>
<form name="oauthForm" class="form">
    <div class="form-group">
        <label for="oauth_redirect_uri">Redirect URI</label>
        <small>Where you expect users to come back to after logging in.</small>
        <input
            id="oauth_redirect_uri"
            name="yourturn_oauth_redirect_uri"
            type="url" />
    </div>
    <div class="form-group">
        <label>Resource Owner Scopes</label>
        <small>{{application.name}} can ask the resource owners for these scopes to be granted:</small>
        {{#each scopes}}
            <label>
                <input
                    type="checkbox" /> {{this.resourceId}}.{{this.id}}
            </label>
        {{else}}
            No scopes available.
        {{/each}}
    </div>
    <div class="form-group">
        <label>Application Scopes</label>
        <small>{{application.name}} has the permission to access data with these scopes:</small>
        {{#each nonOwnerScopes}}
            <label>
                <input
                    type="checkbox" /> {{this.resourceId}}.{{this.id}}
            </label>
        {{else}}
            No scopes available.
        {{/each}}
    </div>
    <div class="form-group">
        <label>Credential Distribution</label>
        <small>Activate credential distribution for these accounts:</small>
        <h6>Amazon Web Services</h6>
        <label>
            <input type="checkbox" /> stups <small>(89257379823523549872345)</small>
        </label>
        <label>
            <input type="checkbox" /> stups-test <small>(2098590257072508527)</small>
        </label>
        <h6>Google Compute Engine</h6>
        <label>
            <input type="checkbox" /> stups <small>(abfafbaf54135413)</small>
        </label>
    </div>
    <div class="btn-group">
        <button class="btn btn-primary">
            <i class="fa fa-save"></i> Save
        </button>
    </div>
</form>