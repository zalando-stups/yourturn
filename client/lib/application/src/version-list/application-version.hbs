<h2>{{app}} Versions</h2>

{{#if versions.length}}
    <ul>
        {{#each versions}}
            <li>
                {{this.id}}
            </li>
        {{/each}}
    </ul>
{{else}}
    No versions to display.
{{/if}}
